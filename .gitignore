# Secrets / local config — never commit a real .env
**/.env
*.tfstate
*.tfstate.backup
.terraform/
.terraform.lock.hcl
*.pem
*.ppk

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node (if a future frontend build step is added)
node_modules/

# OS/editor
.DS_Store

# Personal docs and large binary asset pack — not code, kept local only
/Theisis/
/aws-icons/
/Icon-package_04302026*.zip
