fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
python-multipart==0.0.9
aiofiles==24.1.0
pydantic==2.8.2
pydantic-settings==2.4.0
redis==5.0.8
boto3==1.35.0
httpx==0.27.2
jinja2==3.1.4
lxml==5.3.0
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
PyYAML==6.0.2

# arch2terraform's own runtime deps (imported cross-repo via PYTHONPATH,
# not pip-installed as a package — see Dockerfile's COPY of
# arch2terraform/src) — pinned loosely to whatever arch2terraform/pyproject.toml
# actually requires rather than duplicating exact versions here.
imagehash>=4.3
pytesseract>=0.3
anthropic>=0.120

# Sandbox validation tools invoked as subprocesses by tf_validator.py /
# apply_runner.py. checkov is pip-installable (gets a real `checkov` CLI on
# PATH); terraform and tflint are standalone Go binaries installed directly
# in the Dockerfile, not via pip.
checkov>=3.2
